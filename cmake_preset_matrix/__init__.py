# cmake_preset_matrix
# I built this tool because writing CMakePresets.json by hand is awful
# when you have a bunch of OS / build type / GPU arch combinations.
# This reads a simple YAML config and expands all the combos for you.

__version__ = "0.1.0"
